// Sudoku Quest — Prisma schema
// Les modèles seront ajoutés à l'étape 2.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
